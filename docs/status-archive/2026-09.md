# Archived status entries — 2026-09

Historical release records moved out of docs/STATUS.md to keep that file
bounded. Entries are verbatim and describe the state at the time they were
written; they are not current state. See docs/STATUS.md for that.

# Approved implementation — September 12, 2026

Owner selected TanStack Router + TanStack Query, retaining React/Vite and existing
shadcn/Tailwind/Recharts/Lucide. Atomic work is underway: FX calendar-date correctness
first; router/query Transactions migration and Rodion-only settings in isolated
branches. Add consistent label/control gaps and mobile layout checks. Preserve
backlog and scoped resolver/shadow evaluation; no blanket rule broadening.

FX fix preserves PostgreSQL DATE as text on insert/read instead of shifting through
UTC timestamps. Seventeen focused tests passed locally, one PostgreSQL case awaits
server/CI. No production code change yet. New tests cover UTC/Riga, DST and month/year
boundaries, insert/idempotency and direct/inverse/cross conversion provenance.

# Account policy update and design discussion — September 12, 2026

Applied and verified current-account defaults through the audited account service:
15 formerly unreviewed accounts are personal; six white/FOP accounts remain
business. Wise/Revolut and other personal Monobank accounts retain transaction-level
investment exceptions. No transaction classification or bank record was rewritten.

[Settings, resolver and frontend proposal](../settings-resolver-frontend-plan.md)
records the user's new requests and stack options for discussion before development.
No routing/settings/resolver code was changed. FX investigation found a calendar-date
shift in PostgreSQL DATE mapping plus today's unfilled quotes; fixing FX correctness
is the first proposed atomic increment. The warning is not only a missing-provider
problem. Preserve original records; validate recomputed estimates after correction.

# Final pending-review release — verified September 12, 2026

Release **a61819c3e5931d3d200bf564975ef8fb3b9f48cf** is live; PR56 merged
as 53bbd33. Exact-head full CI **34702465945 passed**. Migration 16 is applied.
The existing-version-15 upgrade/idempotency regression passed locally and on the
server. Full restore comparison of all 33 tables, including payment_snapshot,
passed with workers paused. Both family logins/assets, 18 APIs, FX agreement and
3,890 ledger rows verified after the final switch. App, Telegram and all five
polling timers are active. Shared $10 budget remains healthy (2,646 measured,
five legacy, zero uncertain requests at verification).

This resolves the initial upgrade failure recorded below. Pending debit triage,
pre-settlement Telegram clarification and confirmed-refund visibility are deployed.
Receipt categories and images remain shared and verified for both family logins.
Item-level receipt spending analytics is still future-only. No synthetic Telegram
messages or paid model calls were used for tests; the production worker continues
its previously authorized normal classification under the existing cap.

# Immediate review and quiet full refunds — September 12, 2026

Release **93e6203db6597e1598e307576fd1b2c5b694bf0c** deployed; PR55 merged
as 4d165eb. Full CI **34701908078 passed**, 69 focused pending tests across agent
runs, two integrated review tests and nine isolated server tests passed. Both
logins/assets, 18 API checks, FX agreement, 3,890 ledger rows and both receipt
categories verified. All five bank timers resumed. Default browsing hides eight
Rodion and six Katya confirmed full-refund legs; toggle/direct links work. Pending
Needs review includes 66 Rodion/33 Katya across all history (not Telegram backlog).

Post-deploy checks caught a missed existing-database upgrade: payment_snapshot was
only added by fresh initialization. Applied the exact additive column on the
server and restarted Telegram; it is active again. Migration 16 and an idempotent
version-15 upgrade regression test are now implemented and passed locally. The
follow-up release, full CI and post-schema restore check passed (see final checkpoint above). No financial
records were changed by this correction. The earlier pre-column restore passed
all 33 tables; do not treat that as the final new-schema restore verification.

New debits can be categorized before settlement and unclear live payments can be
asked in Telegram. Exact settlement preserves questions; changed evidence is stale.
Financial totals remain booked-only. Monobank rolling 31-day syncs observe hold
changes; the bank determines completion time. Confirmed full refunds are hidden
only with matching current revisions and equal/opposite booked amounts. Receipt
item-level analytics remains design-only. See pending-payment-triage.md, receipts.md
and spending-workflows.md. Current setup docs and roadmap have been reconciled;
AGENTS.md now explicitly requires current documentation at each checkpoint.

# Receipt-based categorization — verified live September 12, 2026

Release **d7f9c98e78d2d92e335d4a05dea736dbc412de91** deployed; PR53 merged as
eba7ab6. Exact-head CI **34701018136 passed**; eight worker tests passed locally
and on the server. Prior feature CI 34700404475 and final 35 receipt/triage plus
24 earlier integration tests passed. Both deployments passed full 33-table restore
comparison. Both logins, assets, 18 APIs, FX agreement and 3,890 ledger rows verified.
The pending receipt-supported payment is now Food / Restaurants (`applied`);
the previously classified restaurant retains its explicit rule (`protected`).
Both outcomes and shared receipt images are visible to both family logins.
Shared $10 budget healthy: 2,632 measured requests, five legacy, zero uncertain.
The corroborated-MCC correction reused the existing proposal; no new extraction.

Future architecture: docs/receipt-item-analytics.md (versioned evidence and exact
reconciled allocations, deposit/refund/FX handling, coverage-aware analytics).
Item-level extraction, allocations and reports remain unimplemented as requested.
Confirmed full-refund visibility toggle is the next requested UI increment.
Pending charges are currently outside Needs review; their status chips are not
custom tags. No custom tags were assigned to the discussed pending payment.

# Receipt categories — live evidence and confidence correction September 12

Release 3ca617b4970119bfd8a1996a77570cbd68d0f3fe deployed after full CI
34700404475 passed, 35 final receipt/triage tests and 24 earlier receipt/HTTP tests
passed on the server; full 33-table restore comparison and both-login/API health
checks passed. Live receipt model identified the broad restaurant category with
0.84 confidence, corroborated by restaurant MCC, but the new blanket cutoff left
it for review. Corrective policy v2 reuses established consumer-MCC confidence
rules and the existing paid proposal; pending status does not prevent a receipt
category, while reporting still uses actual booking status. Eight worker tests
pass, including this case. Correction deployment pending. Item analytics remains
future-only. Shared $10 budget, manual/rule/account protections are unchanged.

# Receipt-informed categories — candidate September 12, 2026

New worker assesses attached receipt evidence once per minute with the existing
shared $10 budget. Exact receipt snapshots have idempotent request keys; pending
and booked debit payments can gain supported broad categories. Receipt-derived
model choices cannot seed the generic merchant cache. Manual decisions, explicit
rules, exclusions and transfers remain protected. Changed/moved evidence, concurrent
workers and bank corrections are rechecked. Receipt page shows category/outcome.
Future item-level analytics architecture is saved in receipt-item-analytics.md;
item allocations/reports are explicitly not implemented.

Classifier and receipt baseline suites passed (30 tests); final worker suite passed
six tests including reattachment, concurrent reservation and oversized input.
Independent review issues fixed. HTTP verification/build/CI/deployment checkpoint
will be updated after completion. No paid test calls or synthetic chat messages.

# Half-hour Wise/Revolut polling — live September 12, 2026

Owner clarified 30-minute polling with six-hour fallback for all connected Wise/
Revolut accounts, including Kate Wise. Add explicit half-hourly opt-in markers;
retain old hourly-marker compatibility, isolated sticky fallback, 24-hour initial
error cooldown and auth/uncertain failure latches. Three timer drop-ins use 30min.
Only verified successful cooldowns may be shortened on rollout; error/fallback
state preserved. Release **89fd4305f0fb73808fd820efd6846a648f8a22dd** deployed;
PR50 merged as 68d49b2. Full CI **34698970518 passed**; build and ten scheduler
tests passed locally and in an isolated server build. Full 33-table restore
comparison passed. All three effective timer intervals verified at 30 minutes;
only verified successful cooldowns shortened. All five bank timers preserved.
Rollback snapshot: /var/lib/private-finances/half-hour-backup-1789223105488444921.
Both logins, assets, 18 API checks, FX agreement and receipt image access passed;
3,890 ledger rows. Budget remains $10 (2,630 measured, five legacy, zero uncertain).
Today's 19:45 Riga follow-up now checks all three connections and error fallback.

Two received receipts are attached and visible to both owners: one automatic match
and one explicit owner-confirmed match to a pending payment. Confirmation uses the
normal audited receipt action. No receipt corresponding to the other reported
payment has been received. Do not interpret the verification helper's historical
`crossOwnerAutoMatches` field as automatic-only: it counts all cross-owner links.

# Kate Wise import and polling — live September 12, 2026

Application is now active; fresh Katya Wise LV consent expires September 22,
2026 at 14:04:49 UTC. A fresh local backup preceded the read-only 2026 import
(2025-12-31 22:00 UTC through September 12 14:05 UTC). It succeeded for two
accounts: EUR returned 12 transactions dated January 7–September 7; USD returned
zero. Completed import windows cover both accounts. This is provider-returned
history, not an independent statement reconciliation or proof no records are missing.
The Operations API reports `succeeded`, last success 14:06:37 UTC, no error.
The page is a loaded snapshot; Refresh updates earlier failure status.

Enabled `private-finances-sync@enablebanking-katya-wise.timer` at the default six
hours. Seeded a six-hour cooldown after the successful pilot to avoid an immediate
repeat. No prior Kate latch or cooldown existed; no failure protection was cleared.
Both marker and timer verified; no hourly opt-in added. Other schedules unchanged.
New transactions use existing categorization/budget rules; import itself is not
classification. Cancelled tomorrow's Kate activation reminder; preserved the S3
reminder and today's 19:45 Rodion hourly-trial follow-up. Deployment procedures must
now preserve all five enabled bank timers, including Kate Wise.

# Kate Wise approval blocker — verified September 12, 2026

Read-only provider `GET /application` with Kate's server-side credentials returned
HTTP 200, `active: false`, and the correct Tailscale callback URL. Her key works;
the application is not activated. Current UI shows only a generic start failure.
Kate must open her own Enable Banking application and use Activate by linking
accounts / Link accounts for Wise, then repeat approval in this app as Katya.
Activation and application consent are separate steps. No credentials changed,
no consent was started by this diagnostic, and her import timer remains disabled.
After activation/consent, verify account coverage and import before enabling polling.
Provider instructions: https://enablebanking.com/docs/api/linked-accounts

# Shared family receipts and recent transaction browsing — live September 12, 2026

Both family members can upload, view and attach shared receipt evidence. Matching
searches both owners and can use exact bank-supplied original Monobank amounts
(e.g. EUR receipt / UAH account debit), with unique date/amount/merchant and race
checks. Uploader provenance remains; generic transaction APIs remain owner-scoped.
Transactions defaults to All transactions + This month, including pending payments;
Needs review is explicit. Direct links bypass period filtering, explicit historical
review links remain, and filters persist in the URL. Receipt picker searches both
owners with clear owner/pending labels. Telegram privacy is verified disabled;
two Kate receipt jobs were received. Setup docs corrected. Integrated build and
15 focused receipt/HTTP/view tests passed locally and in an isolated server build.
Exact-head CI **34696339208 passed**; PR47 merged as 93c3078. Release
**c74c0bc1d0657de90e548a7c40e34c8671b7cf09** deployed with a full 33-table
restore comparison. Both logins see the same two receipts and can load the images;
one automatically matched across owners, one remains pending for reliable evidence.
Minimal family candidates and generic transaction ownership isolation verified.
Both logins, assets, 18 additional API checks, transaction details and reporting FX
agreement passed; 3,878 ledger rows. App/Telegram verification passed. The monthly
$10 cap remains healthy (2,627 measured requests, five legacy, zero uncertain).
No paid test calls or synthetic Telegram messages. Receipt extraction used the real
photos already received; no resend is necessary.

# Hourly Enable Banking trial and prompt questions — live September 12, 2026

Release **ba741f91761b5f64640a196313fdf154b4713f23** is deployed, PR45 merged
as 9c8fa60. Exact-head CI **34694431231 passed**, including PostgreSQL. Integrated
build, formatting and 22 focused scheduler/Telegram tests passed locally and in
an isolated server build. Guarded deployment and full 33-table restore comparison
passed. Both owner logins, assets, 18 API checks, FX agreement and 3,877 ledger rows
verified. App and Telegram active; $10 cap unchanged (2,625 measured requests,
five legacy holds, zero uncertain). No paid test requests or synthetic chat messages.

Rodion Wise/Revolut now have root-owned hourly opt-in markers and one-hour timer
drop-ins. Only their verified successful six-hour cooldowns were shortened to
last-success plus one hour. No error cooldown/latch was cleared. Next live requests
are 16:20–16:21 Riga; actual hourly acceptance awaits the scheduled follow-up.
Rate-limit/transient errors persist isolated six-hour fallback after the existing
24-hour error cooldown. Empty successful responses do not trigger fallback.
Monobank cadence and disabled Katya Wise schedule remain unchanged.

TELEGRAM_AUTO_QUESTIONS=true and TELEGRAM_LIVE_QUESTIONS_FROM=2026-09-11T21:00:00Z
are verified on the server: today's Riga payments and subsequent booked payments
receive questions after normal triage without a daily five-question limit. Old
backlog stays in the app. Uninspected/processing payments are not questioned;
completed uncertain/deferred review can ask a truthful fallback question. Human,
account and duplicate protections remain. No eligible unsent payments existed at
verification, so real delivery of the new lane remains to be observed.

One-time follow-up: **September 12, 19:45 Europe/Riga**, existing heartbeat
`set-up-s3-backups` (Finance setup reminders and polling check). Original dated
S3/Katya reminders preserved. Inspect provider outcomes/import windows, sticky
fallback and new-payment Telegram delivery, then append a verified checkpoint.
Private configuration rollback snapshot:
/var/lib/private-finances/hourly-trial-backup-1789217321041580186.
See scheduling.md and telegram-setup.md for operating details.

# Prompt Telegram questions — implementation checkpoint September 12, 2026

PF-006: optional `TELEGRAM_LIVE_QUESTIONS_FROM` UTC booking cutoff queues new
unresolved payments after normal triage, without the legacy five-question daily
cap. Current-month pre-cutoff backlog stays in the app. Deferred/uncertain review
can ask a truthful fallback purpose question; uninspected/processing and quiet
suggestions stay out. Account purpose and human decisions remain protected.
No extra AI calls or budget changes. See telegram-setup.md for rollout/rollback.
TypeScript compile and 13 clarification/runtime tests passed with synthetic data.
Deployment, cutoff selection and real delivery verification remain pending.

# Faster bank polling — live September 12, 2026

Release **d5997dbe36e194cbfbbe975812cfe68ef723caad** is deployed; PR43 merged
as 3db9668. Exact-head CI **34693102241 passed**, including PostgreSQL. Build and
nine focused scheduler/backup tests passed locally and in the isolated server build.
Independent review completed; cooldown is rechecked under the invocation lock.
Guarded deployment and full 33-table restore comparison passed. Both owner logins,
frontend/assets, 18 owner/API checks, FX agreement and transaction details verified.

Rolling 31-day imports now end at the current instant, including today's payments.
Both Monobank timers wait 30 minutes after completion, with up to 60 seconds jitter
and one-second accuracy. At the observed seven-minute runtime, expect roughly
37–38 minutes between refreshes. Sequential requests retain 60-second spacing.
Enabled Rodion Wise/Revolut timers wait six hours after completion; a persisted
success cooldown prevents earlier scheduled invocations after restart. This is a
conservative Enable Banking policy; exact bank-specific background limits are not
confirmed. Existing 24-hour failure cooldown and auth/uncertainty latches remain.
Katya Wise remains outside enabled schedules until import coverage is validated.

Timer configuration verified through systemd. Prior unit files saved privately at
/var/lib/private-finances/timer-backup-1789215615387710717 for rollback. Backup
retention keeps 14 recent snapshots and the newest snapshot from 14 distinct UTC
days (at most 28 files), preserving daily history under frequent polling. It cannot
recover snapshots already removed under the old policy. S3 remains deferred.

First live scheduled runs succeeded for all four enabled instances. Both Monobank
runs completed at 15:27 Riga; systemd scheduled the next runs at 15:57–15:58.
Wise/Revolut next runs are 21:20–21:21 Riga. Live import checkpoints include today
through 12:20 UTC; no midnight cutoff remains.

# Recipient details and owner corrections — live September 12, 2026

Release **d46d1bbaf47056b970d59687cead01a35e930a63** is live; PR41 merged as fca12e4.
Exact-head CI **34691184193 passed**, including PostgreSQL.
Data corrections are already applied after a fresh local backup: 27 exact
Kate Baeva transfers in 2026 classified as internal household transfers, including
August 24 35,000 UAH; four OAM purchases classified Groceries. Three exact reusable
rules saved. All other decisions and 2025 records preserved. Private audit receipt:
/var/lib/private-finances/bank-context-corrections-2026-09-12.json.

This release removes the one-off correspondence-search UI and endpoints; future
messenger exports will be analyzed offline for 2026, not connected to the app.
MCC labels expand to 209, including 5311 Department stores. Recipient-bank details
use provider institution names or checksum-valid Ukrainian counterparty IBANs and
an official NBU registry snapshot. Masked counterparty PANs can identify network.
The affected Mono transfers contain no bank/card/IBAN data; UI explains that the
personal API omits information available inside the Monobank app. No guessing.
Integrated build and 13 focused tests passed locally and in an isolated server build;
MCC/historical/triage regression tests passed 39. Guarded deployment and full 33-table
restore comparison passed. Both owner logins, assets, 18 API checks, FX agreement,
all 3,869 ledger rows, corrected transactions and removed endpoint verified live.
App and Telegram services are active. No paid calls: 2,625 measured requests, five
legacy holds, zero uncertain; $10 monthly cap unchanged.

Current review counts: August 14 Rodion + 31 Katya; September 1 + 9. Older 2026
has 289 unresolved rows: 85 tentative estimates and 204 needing context, with two
missing FX conversions. These counts supersede the historical checkpoints below.
Kate Baeva ownership is confirmed and handled; treasury-payment purpose, Katya Wise
coverage and S3 recovery remain outstanding. Messenger exports await the owner.

# Spending workflows — previous release checkpoint September 12, 2026

Release **29153641d7027d3a0290a17ad41188b0528d099b** is live, PR39 (main d5caf34).
Exact-head CI **34689276388 passed**, including PostgreSQL. Local full suite passed
235 with 2 PostgreSQL-only skips, followed by focused estimate, ownership/CSRF,
cutoff and receipt-race tests. Final isolated server build and 15 focused server
checks passed. Guarded deployment, fresh database backup and full 33-table restore
comparison passed. Existing recovery marker permissions were preserved.

Both owner logins, frontend/assets, 18 existing API checks, new review/receipt APIs,
reporting/FX agreement and all 3,869 ledger rows verified. App and Telegram active.
No paid requests made: measured AI requests remain 2,625, five legacy holds,
zero uncertain requests; $10 monthly cap unchanged.

Home opens this month; Analytics opens 2026 with shared currency and separate
historical estimates/month/category views. August review is 20 Rodion + 32 Katya;
current month 3 + 9. Older 2026 has 311 unresolved ledger rows: 85 receive transparent
estimates,226 still need context, 68 exceed 3,000 UAH and 2 lack required conversion.
These estimates never change ledger classifications, rules or confirmed totals.
2025 is archived in views and excluded from automatic triage, not deleted.

Telegram JPEG/PNG receipt intake, bounded shared-budget extraction, private images,
matching/rematching and manual attachment are live. Synthetic tests cover source
correction/duplicate races, unauthorized access, cursor atomicity and budget
concurrency. Real receipt extraction quality still needs an owner photo; PDF,
cross-currency automatic matching and item-level splitting remain unsupported.

Owner-scoped correspondence notes and narrow Gmail search aids are live. Personal
Gmail is connected to this coding assistant; targeted initial searches found no
corroborating messages. Personal Telegram/WhatsApp/Viber/SMS history is not connected
to the application. Await owner choice/export before broader correspondence work.
Treasury-payment purpose and exact recipient Kate Baeva ownership questions remain
pending; no guessed exclusion applied. Kate Wise coverage and S3 recovery remain
outstanding from earlier work. See spending-workflows.md and roadmap.md.

# Spending workflow redesign — in progress September 12, 2026

Candidate worktree /private/tmp/pf-spending-workflows. Live remains d919265.
Integrated Home/Analytics, global currency, review periods and exact daily UAH
priority above 3,000; separate historical estimates with monthly/category tables;
owner-scoped correspondence notes/search aids; Telegram receipt intake, private
images, bounded extraction, matching/rematching and manual attachment. Schema15
extends the shared AI ledger without changing the $10 cap. Archived 2025 records
are retained and excluded from automatic triage; historical Telegram questions
stay in the app. Last full suite passed235 with 2 PostgreSQL-only skips; subsequent
estimate breakdown and HTTP ownership/CSRF tests passed individually. Synthetic
mobile/desktop previews have no horizontal overflow or page errors. Remaining:
final cutoff tests, exact-head CI, server build/deploy and live verification.
No production changes from this candidate yet. No paid API calls made.

# Owner-confirmed context follow-up — September 12, 2026

Data/rules update on existing release d919265; no application code change.
Applied 54 donations, 38 regular psychotherapy classifications, 6 parental-support
classifications, and 54 new exact-description rules. Preserved the 3 documented
insurance exceptions. Linked 6 exact booked same-account full cancellations.
Known descriptions are reusable; unseen names remain an exact-match limitation.
Regular tags were applied to reviewed sessions, not added to future rule actions.

Live category/rule/exception/refund checks passed, plus both-owner access, 18 API
checks and reporting/FX agreement. All 3,869 ledger rows remain; 570 booked outgoing
payments still need review. Measured app requests remain 2,625; zero new paid calls.
Private owner-clarifications and audit receipt append to the frozen reference
under ignored data/categorization-reference-2026-09-12/. The original evaluation
is unchanged. The named psychotherapy mappings are now confirmed; the other
person-specific grocery mapping remains unanswered.

# Reviewed categorization reference — live September 12, 2026

Release **d91926518a7f3e65769262f44b9cee6ddafc0d7f** is live (PR36).
CI **34682075406 passed**, including PostgreSQL. Local suite: 210 passed,
2 database-specific checks covered by CI. Guarded deployment and pre-change
31-table backup restoration passed. Both-owner access, 18 API checks, reporting/FX
agreement and all 3,869 ledger rows passed after the classification changes.

Reviewed all imported rows through 1,123 merchant/MCC combinations, available
bank details and 3,260 unique workbook matches. The frozen private reference and
report are in ignored data/categorization-reference-2026-09-12/. Owner-confirmed
facts, reviewed inferences, ambiguous, incoming and pending states are distinct.
A frozen replay of 1,437 existing unresolved proposals admits 62 additional
reference-matching decisions (6 held-out), with no observed new mismatch. This is
a small cached-policy test, not proof of fresh-model or future accuracy.

Applied 1,042 reviewed classifications and corrected 175 earlier model-only labels
after provenance checks. Added 446 editable reviewed rules in total, including
the explicit bond rule. All named grocery/donation/pet/restaurant examples passed.
The white-card bond received explicit manual Investment attribution while preserving
its pending bank status and business exclusion. Other pending rows were not finalized.
Existing human/refund decisions were preserved; ledger amounts were not changed.

Booked unresolved outflows: **673 combined (Rodion 306, Katya 367)**, down from
1,707. These are not claimed complete. Person-specific workbook explanations still
await confirmation. Kate Wise has no imported rows and remains outside this reference.
Measured app API requests remain 2,625; no new paid calls; $10 budget unchanged.

Normalized MCC corroboration, software/digital contradiction guards, Pets/Donations
starter categories and account investment attribution are deployed. Receipt uploads
are added to the roadmap only. See docs/categorization-reference.md and
account-investment-exceptions.md for method and behavior.

# Incoming transfers and MCC — live September 12, 2026

Release **f1f4417162a455494b8cda577ac034948c10fa7b** is live (PR34).
Exact-head CI **34680632816 passed**, including PostgreSQL. Local suite passed
203 tests with two PostgreSQL checks covered by CI. Isolated server build,
guarded release switch and 31-table backup restore verification passed.
Both-owner access, 18 API checks, reporting/FX agreement and all 3,869 ledger
rows verified after deployment.

The owner-confirmed exact FOP-credit rule is saved with an audit reason and
remains editable. Cached triage processed 93 matching incoming payments with
**zero model calls**; all 93 are own-account transfers (or account-excluded).
The screenshot example was checked explicitly. Existing human overrides remain
protected. This rule does not cover other ambiguous transfer descriptions.

Default spending review now excludes incoming, zero, pending and account-excluded
movements. All-history view remains available; incoming classification is optional.
Rodion's live default queue contains 789 eligible unclassified outflows. This is
an owner-only queue, not a combined-family count or completed reconciliation.

Both providers display readable MCC meanings and supply bounded MCC context to AI.
MCC 4829 is a money-transfer signal and blocks model-only automatic personal-expense
classification; it does not establish account ownership. See docs/mcc-context.md.
Measured AI requests remain 2,625 and the monthly hard cap remains $10.

# Spending reconciliation — live September 12, 2026

Release **1bbbd20df859b27db71d012048742c2cadc42df1** is live (PR32).
Exact-head CI **34679559951 passed199 tests**, including PostgreSQL; initial local
full suite passed195 with 2 PostgreSQL checks in CI, followed by focused model-context
and winter/summer filter checks. Server build, guarded switch and31-table backup
restore verification passed. Both-owner access,18 API checks, converted Overview/FX
agreement and exact monthly sums verified; all 3,869 bank rows preserved.

Overview now combines account currencies via the same daily-rate engine as FX,
with original currency as a distinct filter. Charts and date filters use Europe/Riga.
Incoming money and excluded accounts no longer inflate unresolved/pending spending
counts. Missing conversion and classification coverage remains explicitly visible.

Applied12 audited exact owner-confirmed merchant rules and the specific beverage
correction. Local cached triage revisited363 decisions with **zero model calls**;
171 additional outflows became classified personal spending. Inzhur incoming payments
are investment-related. Named restaurant and sports examples verified individually.
The current pending work is **1,707 unclassified booked outflows and94 bank-pending
outflows**, not a claim that reconciliation is complete. Existing low-confidence,
ambiguous or failed proposals remain unresolved. Saved rules are editable.

Bank transaction details now expose owner-scoped selected sender/recipient, payment
purpose/comment, original amount, MCC, fees and masked bank-provided references.
Missing card references are explained; raw provider payloads are not exposed.
Future model calls receive bounded payment-purpose/name context and amount/currency
clues; account/PAN references are redacted. Existing ambiguous proposals were not
blindly re-requested. Shared$10 budget unchanged; measured requests remained2,625.

The workbook was compared locally using owner/day/description-hash/amount/currency
keys. Same-period gaps include unclassified expenses, unmatched reference rows,
refund/exclusion assumptions and different FX estimates. No workbook uploaded or
imported into the ledger. Further owner clarification requested for repeated
ambiguous transfers; workbook-only assumptions remain unconfirmed.

# Account exclusions — live September 12, 2026

Release **818e39a41fb2b73fd28283370e1e7fcf2d65d962** is live. PR30 merged as
`3cc26fa3d58f9324ac8f3fe3266cd766798a0c86`; exact-head CI **34659126420** passed,
including PostgreSQL checks. Local and isolated server suites passed 186 tests
(two PostgreSQL-only tests covered by CI). Migration14, guarded backup/switch,
31-table restore verification, both-owner access and 18 owner API checks passed.
Synthetic Accounts page rendered successfully; new API tests cover the complete
exclude/restore flow, owner boundaries, exact impact amounts and stale edits.

Both owners explicitly exclude Monobank FOP and white accounts. Six exact registered
accounts now have audited business rules (Rodion four, Katya two), excluding **378**
transactions without changing bank records or underlying classifications. Two stored
personal classifications remain intact beneath the exclusion. Accounts & exclusions
shows purpose, impact, edit reasons and recent history; Review explains rule priority.
Rule changes update report fingerprints. Existing report snapshots remain immutable.
See [account spending rules](../account-spending-rules.md).

All 3,869 rows remain stable across UAH/EUR/USD; monthly sums reconcile exactly.
392 total rows are excluded (378 account-based plus preceding explicit exclusions).
Confirmed UAH conversion coverage is complete; EUR has one and USD two confirmed
expenses without historical commercial rates. Missing rates remain visible.

The prior AI pass was paused safely for this change; `pf-categorize-year-account-rules`
now continues with the explicitly authorized temporary process-only daily limit3000.
Saved daily configuration remains1000; monthly hard cap remains$10. At restart:
measured spend$1.406811, legacy holds$1.59216, remaining after safety reserve$6.501029;
2,095 measured requests, five legacy holds, zero uncertain requests. Business and
investment accounts are skipped before paid requests and cannot train personal
merchant examples. This background pass is ongoing, not claimed complete.

Workbook detail totals reconcile. It remains reference evidence: broader period and
different historical FX assumptions prevent a blind annual-total comparison. Additional
workbook-only large-cash/named-payee exclusions await owner confirmation; not applied.
Private workbook contents and identifiers are absent from Git.

## Previous live checkpoint

# Current status — September 12, 2026

## Live release and verified result

**d389e341e29ad28c6c6aaadc96e22ecb0200a58b** is live. PR28 merged as
`dcf36e815565312376f55b73d7b1397187a16ffd`; exact-head CI **34657361109** passed.
The preceding financial release passed **180 CI tests**, including PostgreSQL;
local and server runs passed 178, with the two PostgreSQL checks covered in CI.
Final refinement changed only frontend/docs; compiled server code was byte-identical.
Guarded backup/switch, both-owner access, budget/expiry and 18 additional owner API
checks passed (slowest 2.11 seconds). The financial migration release also passed
31-table restore verification. Final archive SHA256:
`c1d533646ee8a6d085832e53c239cb6d9796b84de60dc716d6c40345a124a9f5`.

Overview and FX have independent spending-pattern and expense-inclusion filters.
Review supports audited pattern edits and explicit full-refund pairing/unlinking.
Currency conversion preserves all 3,869 rows across display currencies, with exact
Riga monthly owner and family totals. Synthetic mobile dark theme at 390px had no
overflow; refund confirmation and the final family sum were verified in-browser.

Applied exclusions: three July insurance repayments, four eligible investment
outflows, five corroborated foreign own-card transfers, and both entries of one
confirmed full refund. Fourteen ledger entries now have explicit exclusions.
Original bank records remain unchanged. Existing human decisions were preserved.
13 scoped private evidence entries and the original reporting explanations are on
the server; no raw bank data or private source text was added to Git/model prompts.

**FX backfill completed successfully:** 352 transaction dates fetched, 1,053 quotes
stored across 351 dates; one date returned no commercial quotes. No NBU fallback.
All confirmed UAH expenses currently have conversion coverage. EUR/USD still show
missing-rate coverage explicitly. The daily 05:00 UTC timer (up to 10-minute delay)
retries missing dates and imports new dates. Stored rate versions are immutable.

**AI continuation remains active:** `pf-categorize-year-followup.service`.
User approved a process-only 3,000-request ceiling for this historical pass.
Saved daily limit remains 1,000; shared monthly hard cap remains $10. Latest measured
cost was about $0.98, with five conservative legacy holds and one active reservation;
zero uncertain billing records. The pass continues autonomously, with a three-hour
maximum runtime. Its completion must be checked before claiming all eligible
historical transactions have been processed. Uncertain payments remain for review.

Kate Wise reminder: **September 13, 10:00 Europe/Riga**; the existing September 12
S3 reminder is preserved. Kate's fresh Wise consent and off-server S3 backup remain
open. Routine/exceptional labels are editable independently; unspecified patterns
remain explicitly unreviewed. Partial/cross-currency refunds still need review.

## Earlier checkpoints

## Live spending controls and historical conversion

Release **9fe1ace0a075dcd69ca28bb73e7eaffb7618f66c** is live. PR27 merged as
`8ce48244773246bf4ca69261692aed78d72254e6`; exact-head CI34656339346 passed all
180 tests including PostgreSQL. Local/server:178 passed,2 PostgreSQL checks in CI.
Additive migrations12/13, guarded deployment,31-table restore, both-owner access,
budget/expiry and18 additional API checks passed (slowest2.22s).

Applied owner evidence through audited services: three exact July insurance
repayments excluded; one full refund linked; four eligible investment outflows
excluded; five foreign-currency transfers excluded using named own-card suffixes
plus bank MCC4829 and exact per-payment scope. No suffix-only blanket rule.
13 private historical evidence entries now retained on server. Existing human
classifications were preserved; the quoted insurance receipt was not located in
July and no difference was invented as an expense.

Live FX check: all 3,869 rows retained across UAH/EUR/USD; monthly totals reconcile
exactly to converted personal expenses. The daily commercial-rate backfill is
running as `private-finances-fx-sync.service`;352 distinct transaction dates need
coverage. Timer enabled at05:00 UTC plus up to10 minutes. Bank amounts outrank
commercial midpoint estimates; NBU fields are never used. Current totals remain
partial until the rate backfill completes.

First annual AI pass finished2,930 steps. Measured cost$0.664482750 plus conservative
legacy holds$1.592160000; zero uncertain billing records. User explicitly approved
a temporary3,000-request ceiling for remaining historical work. Running unit
`pf-categorize-year-followup.service` has a process-only override; saved daily
configuration stays1,000 and the$10 monthly hard cap is unchanged. Do not change
it permanently. Its final result remains to be checked.

Final frontend refinement67d8bdb adds exact Family totals for each Riga month and
clarifies automatic-review wording; pending its own CI/deployment. Mobile390px dark
preview and synthetic full-refund confirmation passed for the live base release.

Kate Wise reminder: September13 at10:00 Europe/Riga (existing September 12 S3 reminder
preserved). Kate's fresh consent and off-server S3 backup remain open.

## Earlier checkpoints

## Spending controls / daily commercial FX — integration checkpoint

Owner approved PrivatBank archived commercial buy/sell midpoint estimates. Bank
amounts take precedence; NBU fields are excluded. Immutable daily quote storage,
archive import CLI, stable FX transaction list, monthly owner totals, independent
routine/exceptional annotations and explicit full-refund links are implemented.
Overview filters preserve URL state. New schema migrations 12/13 are additive.
Local and isolated server checks: 178 passed, two PostgreSQL checks passed in CI.
Exact-head CI 34656339346 succeeded; PR27 merged as
8ce48244773246bf4ca69261692aed78d72254e6. Release9fe1ace deployment in progress.
Synthetic browser: same six FX rows across currencies, missing coverage, mobile
dark theme at390px without overflow, explicit full refund confirmation succeeded.
See [spending controls](../spending-controls.md) and [FX policy](../fx-policy.md).

Both annual Mono imports completed and exact account/currency coverage was verified;
daily timers were restored. Annual automatic pass completed 2,930 processing steps
with no stop/limit flag. Current aggregate ledger: Rodion 401 personal / 1,623 unresolved;
Katya 354 personal / 1,491 unresolved. 746 automatic audit events, 90 uncertain triage,
1,770 deferred and 1,413 ready. These states are not all finalized classifications;
The1,000/day request guard was reached; shared $10 cap unchanged.
Measured spend $0.664482750, five legacy holds $1.592160000, zero uncertain requests.
Daily request ceiling will be increased within the unchanged dollar cap for the
remaining historical pass, after owner corrections are applied.

July insurance repayments and full refund have unique scoped candidate matches;
corrections await tested deployment. No global recipient or card-suffix rule added.
Kate Wise reminder scheduled September 13 at 10:00 Europe/Riga, preserving the
September 12 S3 reminder. Her fresh consent and off-server S3 backup remain open.

## Previous checkpoint

Latest owner clarification: OpenAI charges belong in **Apps & services / AI tools**.
Applied via the existing audited classification/rule services: nine eligible booked
payments corrected, two owner-scoped exact-description rules saved, no AI calls.
One OpenAI outflow remains pending and was deliberately left outside confirmed
spending. Existing manual decisions and business/investment accounts were excluded.
The general automatic pass still waits for annual Monobank imports. Runtime rule
values and bank records remain server-only; no new application release was needed.

Live release **1a85fb0744a3c846b94b1b36906950bcf8e21efd**, PR25 merged as
`87f3f304235a80cca0868c767ae5c99fed2f23f9`; exact-head CI **34650479677** passed.
Local/server: **152 tests passed**, two PostgreSQL checks covered in CI.
Guarded deployment, 27-table restore, both-owner login, budget/expiry and 18 extra
owner API checks passed (slowest 0.38 seconds). Archive SHA256:
`5f323a06bbf5f01634ee6e786903aae1c3a4254dc17e19cb3dba070dcf0ee1ab`.

The generic model independently recognized two real Playtomic records as
**Sport / Racket sports**, confidence 0.94/0.93. Policy v2 accepts >=0.90 when
supported by consumer MCC evidence, otherwise >=0.95 for ordinary expenses.
Contradictory transfer/exceptional/nonpersonal signals block model application
at any confidence. Human rules/overrides retain priority. Exact safe reuse reduces
paid calls; original proposal, policy, source revision and automatic audit remain.
The $10 monthly hard maximum and $0.50 safety reserve are unchanged. Budget healthy,
47 measured requests, five conservative legacy holds, zero uncertain requests.

**Active server continuation:** `pf-finish-annual-import.service`, verified waiting
for `pf-backfill-monobank-rodion.service` and `pf-backfill-monobank-katya.service`.
Both old-release imports are healthy; coverage checkpoint: 2/7 accounts each.
Rodion Wise 3/3 and Revolut 2/2 requested year windows completed. Current imported
counts: Rodion Mono390, Kate Mono161, Rodion Enable Banking30; these are progress
counts, not a final reconciliation. Range: 2025-09-11T00:00Z–2026-09-11T21:00Z.

On successful Mono completion, the versioned continuation re-discovers current
regular accounts and checks exact account/currency coverage, verifies local recovery,
archives/requeues old Playtomic triage, enables automatic mode and starts
`pf-categorize-year.service` (1,000/day ceiling, unchanged dollar budget).
It independently restores both temporarily paused daily Mono timers on exit.
**No new “continue” is required for that server sequence.** Automatic application
is still off at this checkpoint. Check both continuation and categorization unit
results and aggregate audit/budget state before claiming the pass complete.

**User action remains:** Kate must activate/link Wise in her own Enable Banking
application and create a fresh dashboard consent while logged in as `katya`.
Her prior stored session was created under Rodion's application and returned zero
accounts; it is not usable with her separate PEM. No Kate Wise polling is enabled.
After fresh consent, verify nonzero accounts, import the year promptly, then enable
her timer. July insurance/refund facts still need matching to newly imported bank
records; do not invent matches or global recipient rules. S3 and historical FX
remain the previously documented open work.

## Previous release checkpoint

Live release **85d5b9bfee2cedb35d54c541ab0d020ff2973add**, PR24 merged as
`f32e901e63717c7e6d70ee6255c1785742caeadb`; exact-head CI **34649511359** passed.
Server: 149 tests passed, two PostgreSQL checks passed in CI. Guarded deployment,
27-table restore, both-owner login, dashboard APIs and the unchanged $10 budget
were verified. Archive SHA256:
`66aae689d74cd058a79afab203747902476dba39512541bf9c28bbcbd911246e`.

A two-record live check independently recognized Playtomic as Sport / Racket sports
at confidence 0.94 and 0.93. No Playtomic-specific answer is in the prompt. These are
saved proposals, not ledger classifications: the deployed 0.95 threshold is higher.
A follow-up evidence policy is being tested for >=0.90 suggestions corroborated by
consumer MCCs; unsupported/ambiguous cases remain review-only. Automatic runtime
application is still disabled while the old-release Monobank imports run.

Annual coverage checkpoint at 21:34 UTC: Rodion Wise 3/3, Revolut 2/2;
Monobank 2/7 accounts for each owner. Both year jobs remain healthy. Kate Wise still
needs a fresh session created by her separate application. Do not claim completion.
The server continuation is prepared locally but **not launched**; it will verify
fresh account IDs against year coverage, enable the bounded auto pass, and restore
the temporarily paused daily Mono timers. See the earlier operational details below.

## Previous release checkpoint

Live release **19f2ccb6e570f649055b10e3ff58759abd0d8449**, PR23 merged as
`8383329c32663990247fd9b37cd27c88f4ae8fd5`; exact-head CI **34647230570** passed.
Local/server: 130 tests passed, two PostgreSQL checks covered in CI. Guarded deploy,
27-table restore and authenticated dashboard checks for both owners passed.
Archive SHA256 `7ff8f53a2cc3bc48367f5792b984a08b8c77356f92cd88fbb33aa4559323e260`.
Kate's separate PEM/application pair is configured and verified. Her initial consent
was made during the rollout with the old Rodion application and returns zero accounts;
Kate's own application rejects that session. Fresh authorization requested after
activating/linking Wise in her own application. No Kate Wise polling timer enabled.

The owner now explicitly authorizes automatic categorization of clear cases and
requests at least a year of history from all accounts. Rodion Wise/Revolut full-year
imports succeeded (three/two accounts). Both Monobank year imports are running as
`pf-backfill-monobank-rodion.service` and `pf-backfill-monobank-katya.service`, using
fixed release 19f2ccb and range 2025-09-11T00:00Z–2026-09-11T21:00Z.
Their daily timers are temporarily stopped: **restore both Monobank daily timers
after historical jobs complete or stop**. Do not launch competing Monobank requests.
These jobs checkpoint each complete account, with per-token 61-second spacing.
If a response hits the 500-record completeness boundary, use the forthcoming
adaptive/resumable backfill runner instead of accepting partial coverage.
Automatic application, Playtomic/Sport taxonomy, conservative reuse of repeated
merchant suggestions and resumable annual imports are implemented on
`feat/annual-auto-categorization`; final integrated checks and deployment are pending.
The $10 hard monthly cap is unchanged. Keep automatic application disabled until
the old-release Mono jobs finish: future imports now invalidate AI-only decisions
when bank evidence changes, while preserving human decisions.

Historical Claude conversation “Річні витрати сім'ї” located and inspected.
Owner-confirmed explanations and account clues are saved in a private server-side
evidence file; no ledger classifications changed. Local-only matching and Review
provenance are deployed and verified. July records are
outside current imported coverage, and foreign-card suffixes need fuller matching. See
[historical evidence](../historical-knowledge.md).

Previous release **b1897b4de01153ce7987c7c3f2420a85464527bd**. PR22 merged as
`acf561a37354b1f113f6e59963a6e51786aec5fd` after exact-head CI **34645709779**
succeeded. Server suite: **127 passed, two PostgreSQL-only tests covered in CI**.
The guarded switch and 27-table restore check passed; app and Telegram are active.
Authenticated UI/API, budget and private runtime evidence checks passed. No schema
change. Archive SHA256: `efae952f6e6d65ea67e5c1b0d27035ceeca85e254568fb5f88c7e765fc618a1a`.

One narrowly scoped outgoing investment-recipient context is configured. The only
currently imported matching recipient record is an inflow, so it is deliberately
not matched. No historical suggestions have been applied or fabricated. Foreign
card suffixes and July corrections remain archived pending sufficient bank evidence.
Historical text never enters OpenAI prompts. Overlapping evidence beyond the
three-entry bound stops as uncertain instead of falling through to AI.

Previous release **6c1de13d6d55eca43fbaa0664ae1f28aa0a3f338**. PR21 merged as
`2b6791c312d73354549527854419cf632bb90aa1` after exact-head CI **34642814785**
succeeded. Local/server suites: **121 passed, two PostgreSQL-only tests reserved
for CI**. Migration11 and guarded deployment succeeded; all **27 live tables**
passed disposable restore comparison. Telegram was paused during the switch and
resumed after verification. AWS S3 remains deferred.

Clarification quality fix is live: each owner now has 19 starter categories (both
catalogs were empty), and transactions are interpreted before questions are queued.
Rules and explicit phone signals avoid paid calls; the model receives bounded
merchant-code, account-purpose and previous-owner-decision context. Clear suggestions
appear in Review without a generic question. Automatic application remains off
pending the owner's answer; no suggestions have been silently applied.

A bounded live check of six telecom records produced six successful suggestions:
five Mobile phone, one unresolved; four met the quiet-suggestion threshold.
All six calls have measured usage, the $10 budget is healthy, and all bank records
stayed server-side. Actual bank descriptions use operator names/MCC4814 rather than
an explicit top-up phrase, so the model context improvement matters here. Broader
categorization quality still needs observation; six examples are not general proof.

The shared **USD10 monthly AI maximum** is active, with a **USD0.50 safety buffer**,
Riga calendar months, exact usage costs, atomic maximum-cost reservations and an
anomaly pause. Overview links to the detailed AI spending panel in System health.
Live budget state verified healthy: five legacy requests retain conservative holds;
six measured requests at verification time. Both app and Telegram are active.
See [AI budget](../llm-budget.md) for accounting boundaries and rollback precautions.

All dashboard screens now use the coherent shadcn/ui, Tailwind, Recharts and
Lucide frontend: Overview, Accounts, Reports, Connections, Review, Categories,
FX, System health and transaction history. Mobile navigation and system/light/dark
themes were checked with synthetic data. Authenticated live shell routes, assets,
Overview API, AI/Telegram feature flags and workflow health passed verification.
Private URL: https://<private host>:8443/

Telegram worker is active with the paired group and two owner identities. Replies
now produce bounded AI suggestions; explicit owner confirm/reject is required.
Chat, owner, transaction revision and polling acknowledgment are checked atomically.
Interrupted sends and receipts are visible in System health. Synthetic workflow tests
passed; a real owner reply-to-confirmation cycle remains to be observed.

OpenAI gpt-5.4-mini-2026-03-17 is enabled, capped at 50 requests/day, following a
successful synthetic app-level API probe. The shared monthly dollar cap also applies. Key expiry is recorded as **December 10,
2026** (date only); replacement reminders are **December 5, 8 and 9, Europe/Riga**.
The dashboard reports expiry metadata without exposing key contents.

Four bank polling timers remain enabled: both Monobank owners (regular accounts),
Rodion Wise and Rodion Revolut. Calendar report timer is active. Katya Wise consent
is outstanding. Historical market FX and refund/fee/cash/split policies remain open;
missing rates never silently fall back to NBU or invented values. Refund timing
question is pending with the owner. Encrypted S3 recovery and broader alerts remain
unfinished; the product is usable but these acceptance gaps must not be called done.

Implementation: /private/tmp/finances-app. Canonical project folder:
/Users/rodik/ChatGPT/PersonalFinances, synchronized with this checkpoint.
Repository: sars/private-finances. No real financial records or credentials copied
locally for frontend verification.

## Earlier implementation notes (historical checkpoints)

# Development handoff

Updated: 2026-09-11. Branch: feat/classification-workflows.

## Current verified state

Private dashboard: https://<private host>:8443/ through Tailscale
Serve, with owner-specific Basic authentication. Production release e37ddb2
is deployed after green CI and server tests: 34 tests passed locally/on server;
the additional PostgreSQL integration test runs in CI. Schema version 4.
Per-bank scheduling merged in PR #15 as 72e26b3; consent host fix is included.

Rodion authorized Wise LV and Revolut LV; both expire on 2026-09-21 around
17:46 UTC. Server-only imports succeeded for all three Wise accounts and both
Revolut accounts. The September 10 UTC window returned no transactions; the
August 12 through September 10 UTC window imported one Wise and five Revolut
records. Replaying the same window returned changed=0 for each bank, proving
idempotency for these live records. No bank payloads were copied locally.
Katya's Enable Banking consent remains outstanding.

Monobank September 10 UTC pilot: Katya succeeded across seven account windows
with six records. Rodion completed seven regular account windows with two records;
the remaining jar statement returned HTTP 400. Coverage is incomplete and the
failure remains visible; the jar was not silently excluded.

All bank credentials and consent session files are restricted, server-only files
outside Git and release directories. Verified local bank-secret duplicates were
removed. Use ~/.local/bin/pf-gh for repository-scoped GitHub access; the former
broad GitHub CLI OAuth authorization was revoked.

## Implemented product and operations

Owner access, exact integer amounts, pending/booked status, provenance,
idempotent imports, classification overrides and audit history are implemented.
Dashboard supports date/currency/category filters, unresolved totals, connection
consent and operations status. Provider I/O is read-only, bounded and sanitized.
Per-account import windows and renewable exclusive leases persist in PostgreSQL.
Secrets and raw transaction details are excluded from general logs and UI APIs.

Isolated release deployment takes a local database dump and checks authenticated
readiness. Manual compatible rollback was exercised successfully. Automatic
failure-triggered rollback is implemented but not failure-injection tested.

## Recovery and scheduling limits

Amazon S3 backup setup is deferred at the owner's request; a reminder was set.
A fresh private local dump preceded Enable Banking pilots. An earlier restore
exercise verified schema and Monobank pilot aggregates in a disposable database.
Fresh local recovery of the expanded dataset passed: all eight application tables
matched after restoring into a disposable server database, which was then removed. Off-server
encrypted recovery is not configured or verified.

The owner authorized bounded live pilots despite deferred S3 setup. Off-server
backup is not a bank prerequisite. Recurring timers remain disabled. The scheduler accepts truthful local or off-server recovery proof plus per-instance
enablement; do not describe polling as live.

## Per-bank scheduling checkpoint

34 local/server tests passed; the additional PostgreSQL integration test passed in CI.
Independent read-only review found no blocking issues. Tests prove one bank's
consent failure/lease does not block or overwrite the other, and schedules select
the proper session. Legacy combined status is labeled explicitly. CI run 34630742166 passed.
Release 037e20e deployed with a fresh predeploy dump and authenticated readiness.
No old bank import processes were running. Live replays selected each bank via
CLI argument and returned changed=0 across three Wise and two Revolut accounts.
Authenticated /ops verification confirmed both separate banks and legacy labeling.
Archive SHA-256: de564a7c59591a9048067fb732c7b93be6e07807fe3fae7d4ee4e85f2b2993c1.

## Local recovery and polling preparation

36 local tests passed, one PostgreSQL-only integration test awaits CI.
Local recovery marker recorded after comparing all eight restored tables.
New pre-import local backup service atomically retains 14 successful dumps; import
units require its success. S3 remains deferred; no off-server proof was fabricated.
Both Mono tokens report psfjm permissions; Rodion's listed jar still needs provider
resolution. Potentially capped Mono responses (500+ entries) now fail incomplete
instead of silently claiming full account coverage. Polling release 284245c deployed after green CI and server tests. Backup failure
was exercised and preserved every existing dump. Three daily timers are enabled:
monobank-katya, enablebanking-rodion-wise, enablebanking-rodion-revolut.
Both Enable Banking scheduled runs succeeded. Katya's initial scheduled run is
still processing. Rodion's timer awaits explicit regular-accounts-only release.

Registry/report increment: 44 local tests passed, one PostgreSQL test reserved
for CI. Migrations 5/6, discovered account registry, owner-editable identifiers and
purposes, conservative transfer suggestions, report versions and web controls are
implemented. Riga periods approved. NBU rejected; no FX source silently substituted.
Explicit Mono regular-account scope is implemented and visible; not yet deployed.

## Next implementation

1. Per-bank sync identities and scheduler instances are deployed and verified.
   No timers enabled; operational rollout of recurring polling remains pending.
2. Verify recovery of the expanded live dataset using a disposable server database.
3. Resolve Monobank jar failure and verify handling of full/truncated statement pages.
4. Complete scheduling policy and consent-expiry visibility before enabling timers.
5. Continue account registry, transfer matching, categorization, reports and Telegram.
   Historical FX, refunds/fees/splits and report cutoff still require implementation
   and relevant product decisions. Never invent FX rates or count pending as booked.

## Working agreement and locations

Commit small verified increments and push useful checkpoints with current docs.
Prefer CLI/API, concise English and short outputs. Keep real financial data and
credentials on the server. Do not restart unrelated server applications.

Implementation checkout: /private/tmp/finances-app.
Canonical project: /Users/rodik/ChatGPT/PersonalFinances.
Repository: https://github.com/sars/private-finances.
Server releases: /opt/private-finances/releases; current symlink selects release.
Service: private-finances.service. Credentials: /etc/private-finances/credentials.
Consent sessions: /var/lib/private-finances-consent.

## Visible product and workflow checkpoint

Own accounts and Reports deployed in e37ddb2 after CI and server tests. Fresh
restore verified all 11 tables. Four daily bank timers now enabled and completed
successful runs: Monobank for both owners (regular accounts only), Rodion Wise,
Rodion Revolut. Jar exclusion is explicit; historical records are never deleted.

Next release implements Categories/Review, exact confirmed rules and hierarchy,
tags, structured bounded AI proposals, Telegram queues/replies/report delivery,
calendar catchup and partial display-currency analytics. Live AI is disabled
without a configured key/model and demo mode ignores all live credential paths.
Telegram token is installed and getMe verified; group/user setup messages and
owner mapping remain pending. No financial Telegram messages sent.

81 tests passed in the full bounded-concurrency run; latest starter-category and
auto-question wiring receive targeted checks before CI. Review caught and fixed
mixed-revision FX evidence and demo live-credential activation. Missing FX stays
visible; NBU is not used. Credential origin/renewal guide: docs/credentials.md.

## Overview and credential-expiry checkpoint — September 11

Current live release remains e37ddb2. All four bank timers have succeeded.
Workflow head a02427b passed GitHub CI 34635186453 and the server build
(85 passed, one PostgreSQL-only test skipped). It has not been deployed yet.

Overview now uses React, official shadcn/ui components, Tailwind, Recharts and
Lucide. It consumes one authenticated /api/overview response for consistent
transactions/totals, retains owner/date/currency/category filters and history links,
and supports system/light/dark themes and responsive navigation. Other routes retain
their existing server-rendered screens until each replacement is ready. No Tremor
or TanStack Table was added. Local preview uses synthetic data only.

Telegram group and both owner identities have been paired. No financial messages
have been sent; the worker is not enabled yet. OpenAI key was saved on the server
and its file permissions verified as 600, service-owned, without reading its value.
User supplied expiry December 10, 2026; reminders December 5, 8 and 9 in Europe/Riga.
Credential monitoring and durable deduplication are implemented with synthetic tests,
but require server rollout/configuration. Schema migration 8 includes reminder history.
AI key/model activation and a fresh expanded-dataset restore remain rollout steps.

Next: finish visual/functional Overview verification, commit/push, exact-head CI,
server build and guarded deployment, fresh restore, then enable configured Telegram
and key monitoring. Continue secondary-screen redesign and remaining product work.

Overview verification: 89 local tests passed, one PostgreSQL-only test skipped
for CI; production frontend build passed. Browser checks covered desktop dark and
light, 390px mobile layout, synthetic import/classification, chart population,
owner filtering with persisted URL, and zero browser console errors. Review found
the eight-row list lacked access to older same-day records; Show more now exposes
all matching rows. Final layout adjustments received a fresh build.

## Full frontend and Telegram confirmation candidate

All dashboard routes now use the same responsive shadcn/Tailwind shell, including
Review, Accounts, Reports, Categories, FX, Connections, History and Operations.
Views load separately to keep navigation payloads smaller. Mobile navigation uses
a focus-managed Sheet; filters and forms remain accessible. Synthetic browser
checks verified account save, report creation, starter categories, confirmed rule
creation, classification and exact FX/missing-rate display.

Review found and fixed full category paths exceeding the 80-character classification
limit. Backend validates descendants transactionally; model suggestions now use
canonical hierarchical paths too. Workflow monitoring exposes counters and interrupted
sends without message or financial contents.

The Telegram reply workflow is implemented: an owner's answer yields a bounded
AI suggestion; replying confirm/reject to that proposal is owner/chat/revision
checked and committed with polling acknowledgment. Receipts are durable. No new
rule is learned automatically. Migration 9 must run before the new classifier,
and expanded recovery must pass before resuming the worker. Candidate not deployed.

Candidate verification: combined suite 99 passed, one PostgreSQL-only test
reserved for CI. Final confirmation category-lock and recovery-visibility changes
passed seven targeted tests. Mobile Sheet navigation closes with Escape; dark
mode works; final browser console showed no errors. No real transactions were
copied locally for UI testing.
