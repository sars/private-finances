# Template only; main.ts reads process environment, not this file automatically.
APP_MODE=demo
PORT=3300
RELEASE_SHA=local
# For isolated PostgreSQL testing, set APP_MODE=postgres and provide these outside Git.
DATABASE_URL=
RODION_PASSWORD=
KATYA_PASSWORD=
