# Template only; main.ts reads process environment, not this file automatically.
APP_MODE=demo
PORT=3300
RELEASE_SHA=local
# For isolated PostgreSQL testing, set APP_MODE=postgres and provide these outside Git.
DATABASE_URL=
# Each owner signs in with an address and a password. Both are applied to the
# users table on every boot, so rotating a password is edit-and-restart.
RODION_EMAIL=
KATYA_EMAIL=
RODION_PASSWORD=
KATYA_PASSWORD=
