import { PageHeader } from '@/components/finance';

/** Placeholder until the snapshots page lands: every snapshot date, and a form for today's manual figures. */
export default function AssetsSnapshots() {
  return (
    <div className="mx-auto max-w-7xl space-y-6 pb-8">
      <PageHeader title="Snapshots" description="Coming in this release." />
    </div>
  );
}
