import { PageHeader } from '@/components/finance';

/** Placeholder until the edit page lands: one holding's settings on a page of its own. */
export default function AssetEdit() {
  return (
    <div className="mx-auto max-w-3xl space-y-6 pb-8">
      <PageHeader title="Holding" description="Coming in this release." />
    </div>
  );
}
